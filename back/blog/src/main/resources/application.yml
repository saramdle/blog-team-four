spring:
  profiles:
    active: dev # 개발환경
#    active: prod # 운영환경
    include:
      - oauth
      - aws

logging:
  level.org.hibernate:
    SQL: debug
    type.descriptor.sql.BasicBinder: trace

upload:
  dir: 'your local dir url'
