spring:

  jpa:
    hibernate:
      ddl-auto: create

  profiles:
    include: oauth

logging:
  level.org.hibernate:
    SQL: debug
    type.descriptor.sql.BasicBinder: trace

upload:
  dir: 'your local dir url'